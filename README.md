# RFC-8 playground

This repo is used to experiment with implementing different datasets using NGFF RFC-8 collections and extensions. The scripts to produce the datasets are in `examples/`. There are some common models and utilities for working with the metadata in `src/rfc8_playground`